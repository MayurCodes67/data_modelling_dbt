version: 2

models:
  - name: fact_weekly_customers_churn_rate
    description: >
      churn_rate = rate of churned customers.
      Used (churned_customers / active_customers_prev_week).
      
    columns:
      - name: week
        tests:
          - not_null
