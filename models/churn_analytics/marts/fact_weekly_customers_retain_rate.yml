version: 2

models:
  - name: fact_weekly_customers_retain_rate
    description: >
      retain_rate = rate of retained customers.
      Used (retained_customers / active_customers_prev_week).
      
    columns:
      - name: week
        tests:
          - not_null
