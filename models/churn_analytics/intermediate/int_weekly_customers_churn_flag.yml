version: 2

models:
  - name: int_weekly_customers_churn_flag
    description: >
      Churn flagging = customers who were active prev week but not this week.
      Used CASE to produce 1/0 flag.
    columns:
      - name: customer_id
        tests:
          - not_null

      - name: week
        tests:
          - not_null