version: 2

models:
  - name: int_weekly_active_customers
    description: >
      Finding active customers this week.
      Used(1 AS is_active).
    columns:
      - name: customer_id
        tests:
          - not_null

      - name: week
        tests:
          - not_null