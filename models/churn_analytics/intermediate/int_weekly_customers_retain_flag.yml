version: 2

models:
  - name: int_weekly_customers_retain_flag
    description: >
      Retain flagging = customers who were active both prev week and this week.
      Used CASE to produce 1/0 flag.
    columns:
      - name: customer_id
        tests:
          - not_null

      - name: week
        tests:
          - not_null