version: 2

models:
  - name: int_weekly_active_customers_lag
    description: >
      Finding active customers prev week.
      Used lAG(is_active).
    columns:
      - name: customer_id
        tests:
          - not_null

      - name: week
        tests:
          - not_null